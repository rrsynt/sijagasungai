# Keys yang di-embed di client-side bundle
# PENTING: Set nilai ini di environment variables platform deployment (Cloud Run, Vercel, dll)
# JANGAN isi key di sini — file ini ter-commit ke repository
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
